import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export const metadata = {
  metadataBase: new URL('https://ferramentas360.com.br'),
};



export default nextConfig;
